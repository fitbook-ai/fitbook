services:
  - type: web
    name: fitbook
    runtime: node
    buildCommand: ""
    startCommand: node server.js
    plan: free
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_VERSION
        value: 22.0.0
